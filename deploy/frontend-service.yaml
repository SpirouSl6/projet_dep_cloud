apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: LoadBalancer
  loadBalancerIP: 20.19.160.31
  selector:
    app: frontend-app
  ports:
    - port: 80
      targetPort: 80
